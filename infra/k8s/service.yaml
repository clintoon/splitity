apiVersion: v1
kind: Service
metadata:
  name: splitity
  labels:
    app: splitity
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 8080
  selector:
    app: splitity
  type: NodePort
