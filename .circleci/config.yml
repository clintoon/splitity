version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10
    steps:
      - checkout
      - run:
          name: pwd
          command: pwd
      - run:
          name: Install dependencies
          command: yarn install
      - run:
          name: Run jest unit tests
          command: yarn jest
